import 'package:flutter/material.dart';

class PurchaseOrdersSupplychain extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.amber.shade300,
    );
  }
}
