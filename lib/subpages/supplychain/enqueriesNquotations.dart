import 'package:flutter/material.dart';

class EnqueriesQuotationsSupplychain extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.amber.shade300,
    );
  }
}
