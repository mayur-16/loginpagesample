import 'package:flutter/material.dart';

class Deliveryeshowroom extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.deepOrange.shade300,
    );
  }
}
