import 'package:flutter/material.dart';

class TimeNattendencesmc extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.yellow.shade400,
    );
  }
}
